import React from 'react';
import { shallow } from 'enzyme';
import ProductList from './ProductList';

// declarations of mock props, components etc

beforeEach(() => {
    // declarations of mock functions and props used in the tests here
    // shallow rendering of components (example below)

    // myComponent = shallow( <MyComponent myProp={ mockProp} /> );
});

afterEach(() => {
    // mock resets of functions etc
});

it('should render.... XXX', () => {
    expect(true).toEqual(true);
});

